document.getElementById("thumbnail").addEventListener("click", function() {
    var fullsizeImg = document.getElementById("fullsize");
    fullsizeImg.style.display = fullsizeImg.style.display === "none" ? "block" : "none"; // Toggle visibility
});
